import { Formik, Field, Form, ErrorMessage } from 'formik';

const Contact = () => {
  return (
    <Formik>
      <Form>
        <label htmlFor=''></label>
        <Field />
        <ErrorMessage />
      </Form>
    </Formik>
  );
};

export default Contact;
